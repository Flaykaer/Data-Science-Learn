1. Написать программу, которая выводит текущую дату в полном русскоязычном формате, например: “Сегодня 8 декабря 2018 года, суббота, 20 часов 6 минут 54 секунды”. Следует обратить внимание на правильные склонения слов “час”, “минута”, “секунда”. 

Продвинутый уровень: с помощью стандартной функции setInterval сделать так, чтобы сформированная строка с датой и временем отображалась в консоли каждую секунду.

2. Написать простую программу авторизации. Создать массив users, в котором каждый элемент – это объект типа “пользователь” со свойствами “имя”, “логин” и “пароль”. Программа должна запрашивать у пользователя логин и пароль при помощи prompt, после чего проходить по массиву users и искать в нем объект с такими же значениями. Если пользователь найден в массиве users, то программа должна поздороваться с ним по имени. В противном случае должна появиться ошибка авторизации.

function time(){
    let date = new Date();

    let month = date.getMonth()
    switch (month) {
    case 0:
        month = 'Января'
        break;
    case 1:
        month = 'Февраля'
        break;

    case 2:
        month = 'Марта'
        break;

    case 3:
        month = 'Апреля'
        break;

    case 4:
        month = 'Мая'
        break;

    case 5:
        month = 'Июня'
        break;

    case 6:
        month = 'Июля'
        break;

    case 7:
        month = 'Августа'
        break;

    case 8:
        month = 'Сентября'
        break;

    case 9:
        month = 'Октября'
        break;

    case 10:
        month = 'Ноября'
        break;

    case 11:
        month = 'Декабря'
        break;

}
    let week = date.getDay()
    switch (week) {
    case 0:
        week = 'Воскресенье'
        break;
    case 1:
        week = 'Понедельник'
        break;
    case 2:
        week = 'Вторник'
        break;
    case 3:
        week = 'Среда'
        break;
    case 4:
        week = 'Четверг'
        break;
    case 5:
        week = 'Пятница'
        break;
    case 6:
        week = 'Суббота'
        break;
}
    alert("Сегодня "+date.getDate()+" "+month+" "+date.getFullYear()+" год "+week+" "+date.getHours()+" час"+''+date.getMinutes()+" минут "+date.getSeconds()+"секунды")
}
setInterval(() => time(),1000);



2




class Person {
    constructor(name,login,password) {
        this.name = name;
        this.login = login;
        this.password = password;
    }
}
const users = [new Person('Oleg','1','2')]
userLogin = prompt('Введите логин');
userPassword = prompt('Введите пароль');
let name = ''
if (users.find(item => {
    if (item.login == userLogin && item.password == userPassword){
        name = item.name
        return true
    }
} )){
    alert('Дратути'+name)
} else {
    alert('error')
}
